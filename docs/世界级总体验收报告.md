# 世界级总体验收报告

更新日期：2026-05-21

## 当前结论

`微关系动力学全息` 已完成本轮世界级支柱闭环：数据生命体、训练掌握、关系感知、安全治理、前端沉浸、AI 质量和工程门禁均已落地，并可由 `tools.commander` 读取 DAG 自动选择下一动作。

当前阶段执行重点已调整为：优先保证网站能正常启动、正常访问和核心路径可用；更深的安全治理、严格生产化和非阻断审计细化暂缓到项目功能全面定型后集中处理。

## 已具备能力

- 数据：来源登记、metadata-only 抓取、脱敏、去重、标注任务、训练资产版本、内容来源归档、导入质量审计，以及样本/资源/知识的 reviewed/published 统一发布治理。
- 数据修复：导入质量报告已具备 repair plan，支持 dry-run/apply 安全回填样本 provenance、资源 scene/format hint 和知识 source metadata，并以 SQLite 字段和 `repair_source` 作为审计主真源。
- 数据债务：导入质量报告已新增 `quality_debt`，区分可自动修复字段缺口与需人工/来源治理的历史 import issue；共享库安全回填后自动修复项已清零，剩余转为来源级复核。
- 导入 issue 治理：历史导入问题已具备 active/open/review_requested/resolved/reopened 状态机、reviewer、resolution hash、resolved_at、updated_at、来源分组、来源级复核包、批量选择、批量关闭报告、审计历史查询和 `pipeline_run_logs` 审计；resolve 必须人工说明，系统不允许自动刷掉来源问题。
- 导入 issue 审计安全：队列和审计历史只回显状态流转、reviewer、来源元数据、严重度、resolution hash 与安全标记；新治理日志的 `result_json` 和 `message` 均不保存人工说明明文或第三方来源原文。
- 发布治理：Reviewed 资产发布候选队列已输出质量信号、优先级理由、publish_ready 和人工确认门禁；发布操作台支持 dry-run、确认发布、撤回和复审请求，所有状态变化写入 `pipeline_run_logs` 审计。
- 发布治理前端：独立 `/governance` 页面已支持 reviewed 候选查看、dry-run 预演、确认发布、撤回和复审请求；同页已接入导入 issue 复核队列、来源优先级、状态筛选、批量选择、批量报告、审计历史、dry-run、关闭、复审和重开，真实状态变更仍必须通过 API 写入 SQLite 与审计日志。
- 学习：八阶课程图谱、样本多粒度标注、Gold Set 脚手架、结构化 diff、掌握阶段、错题归因、间隔复习和推荐上下文。
- 校准：Gold Set 已具备专家复核摘要、复核队列、复核提交、版本留痕、跨审阅者一致性指标和 Dashboard 校准台，评分基准可持续从脚手架升级为专家证据层。
- 校准治理：Gold Set 冲突队列已按决策分歧、评分差、信心差和最新审阅版本生成复议优先级，并纳入 commander 回归审计；共识冲突关闭已新增 `consensus_review` 版本，保留历史专家分歧，当前开放冲突为 0。
- 关系感知：情绪温度计、情绪流、需求雷达、边界色带、互动回路、轻验证问题和 AI 伴侣关系状态轨迹。
- 安全：PUA、操控、胁迫、跟踪、危机风险硬阻断；安全事件和 AI 运行日志均保存哈希、摘要和处置结果。
- 前端：Dashboard、Trainer、TrainerAI、Path、Mistakes、Knowledge、Evolution、Framework 已通过桌面/移动浏览器 smoke。
- 资源运营：资源库已扩展到 12000 条且 `content_fingerprint` 精确重复债务为 0；资源海洋支持分页、关键词、标签、来源、类型和场景检索，并显示原文级导读、许可边界、质量分和原站链接；默认视图优先展示 4206 条具体案例训练卡，每条包含场景、TA 原话、常见失误、更好回应、情绪信号、边界与练习任务。
- 资源使命对齐：资源生成已从泛研究/泛统计入口纠偏为微关系训练主线，核心围绕对话、情绪流、边界同意、暧昧张力、冲突修复、长期连接、幽默互动和错题改写；背景源保留在浏览冲浪，不再主导资源海洋。
- 浏览冲浪：浏览冲浪页已扩展到中英文混合来源目录，标题本身可点击跳转原站，并展示网站摘要、内容结构、质量说明、链接状态、主题入口和右侧页内目录。
- 训练活性：AI 伴侣前端已展示 DeepSeek provider 状态，错题本已把错因扩展为情绪流动动线、三步改写练习和关联资源检索。
- 启动体验：`start.sh` 可清理本项目残留 3000/8000 旧服务，默认无 reload 启动后端，启动后检查 `/health`、分析中心 API 和前端首页再输出访问地址。
- AI：DeepSeek/OpenAI-compatible 调用可降级；提示版本、schema 版本、Provider 结果、安全阻断和 fallback 结果可审计。
- 分析：AI 质量哨站、AI 失败根因聚类和跨会话关系趋势已可审计，可观察成功率、降级率、安全阻断、Provider 失败、fallback reason、修复指数和长期状态增量。
- 分析中心：独立 `/analytics` 页面已聚合 AI 质量、Provider 诊断、Gold Set、导入质量、向量召回、训练趋势和生产调度健康，提供分数卡、告警队列、历史状态线、分域详情、任务 freshness 与恢复 runbook，并保持不返回 payload/私密原文/第三方全文的审计边界。
- Provider 治理：AI Provider 诊断已可输出脱敏配置、mode/model/base_url/chat_path、兼容性检查、失败率、HTTP status 分布、风险等级和处置建议，并纳入 commander 回归审计；默认 DeepSeek 配置已硬化为 OpenAI-compatible `https://api.deepseek.com` 与 `deepseek-v4-pro`。
- Provider 请求形态：诊断已输出脱敏 request_shape，包括 URL path、payload 字段集合、message roles、schema hash 和 compatibility risks；当前真实配置 request_shape 无本地形态风险，HTTP 400 更可能来自账号能力、模型授权、区域或服务侧请求约束。
- Provider 探针：`POST /api/analytics/ai-provider-probe` 已支持默认 dry-run 的最小实时探针外壳，并把探针结果写入 `ai_provider_probe_logs`；审计只保留 request_shape、状态码/错误类别和延迟；真实 live probe 还需要 `DEEPSEEK_LIVE_PROBE_ENABLED=true` 显式授权，否则返回 409 并记录 blocked_by_policy。
- Provider 运行手册：`GET /api/analytics/ai-provider-probe-readiness` 已提供不外呼的 readiness/runbook，输出 configured、live_probe_enabled、blockers、脱敏 request_shape、recent probe logs、quality gate 和 dry-run-first/live-enable/live-probe 步骤；分析中心已展示 Live Probe Readiness。
- Provider 失败处置：Provider 诊断已新增 failure playbook，可把 missing key、本地 request shape 风险、HTTP 400 账号/区域/模型授权或服务侧约束、timeout、network error 等聚合失败映射为根因矩阵、回归样例、安全 runbook 和质量门禁；当前真实 HTTP 400 在本地 request_shape 无兼容风险时会明确归类为 account/service 风险，而不是误导为继续核对本地配置。
- 自动化：DAG、状态快照、每日自动推进任务、周进化 dry-run、指挥官验证入口、APScheduler 长期调度入口、项目内 launchd/systemd 服务配置、调度健康告警、恢复手册和分析中心可视化已可持续运行；调度 run-once 会写入本地审计快照和 health 摘要，服务配置生成到审计目录后再由人工/部署流程安装。
- 数据库治理：正式 migration runner 已支持计划、dry-run、备份、审计记录和数据库健康报告联动。
- 数据库 revision：migration runner 已具备具体 active revision，可重建元数据向量索引、同步 sqlite-vec 镜像、归一化导入 issue 状态并把结果摘要写入 `formal_migration_revision_results`。
- 检索：持久化元数据向量索引已覆盖 raw content、knowledge、sample、resource；sqlite-vec 原生 ANN 后端已接入，支持 vec0 镜像表、KNN 查询、后端状态报告、本地签名安全降级和 ANN 召回评测。
- 资产治理：资源和知识条目已具备 review/publish 状态字段，进化中心可统一汇总并回填三类资产的发布状态。

## 当前门禁

- 后端：`.venv/bin/python -m pytest -q`，110 passed，产品覆盖率 85%；Gold 共识关闭、AI Provider 兼容性、Provider 探针、导入质量回补、导入 issue 治理/审计历史、Reviewed 发布操作、APScheduler 调度、独立分析中心、发布治理前端和导入 issue 前端治理专项通过。
- 代码规范：`.venv/bin/python -m ruff check backend tests tools` 通过。
- 前端：`npm run type-check && npm run build && npm run smoke:world` 通过。
- 指挥官：`docs/tasks/module_dag.json` 56/56 完成，`docs/tasks/commander_state.json` 可记录下一状态；`regression-audit` 已包含独立 `import_quality`、`reviewed_publish_candidates`、`gold_conflict_queue` 与带 request_shape 的 `ai_provider_diagnostics` 检查；import quality 快照已输出 active/resolved issue 数；`docs/tasks/production_scheduler_state.json` 记录长期调度审计快照、健康状态、告警和恢复手册；分析中心已读取同一审计快照展示生产调度健康；`docs/tasks/scheduler_service/` 保存 launchd/systemd 服务化审计文件。
- 数据库：真实库 `status=ok`，`quick_check=ok`，缺表/缺列为空，`metadata_vector_index` 已有 1196 行且向量 JSON invalid=0。

## 已知限制

- 来源抓取保持合规保守策略，只保存标题、URL、hash、风险评分等元数据，不保存第三方全文。
- 向量检索已接入 sqlite-vec 原生后端；ANN 召回评测与阈值校准已落地，后续可继续建立跨表重复判定和长期回归计划。
- Gold Set 已有专家复核闭环、跨审阅者一致性报告和 consensus conflict closure；仍需要持续提高真实专家覆盖率、样本规模和真实多审覆盖，避免把共识关闭误解为外部专家覆盖增长。
- AI Provider 配置形态、request_shape、失败处置矩阵、dry-run 最小探针、live probe 显式授权安全门和 readiness runbook 已硬化，近期运行日志仍显示 HTTP 400 高频；当前证据显示本地请求形态无兼容风险，真实外部探针需要有效凭证、模型授权、账号区域确认和 `DEEPSEEK_LIVE_PROBE_ENABLED=true`，不能把 dry-run/readiness/playbook 误报为生产稳定。
- 导入质量自动修复项已清零，当前 89 条 active、11 条 resolved 历史来源/人工复核 issue；已有关闭/复审状态机、来源分组、来源级复核包、批量关闭报告和审计历史，且正式迁移已把空状态归一化为 `open`，但仍需要真实来源核对或人工确认，不能通过生成假元数据或发布绕过来刷分。
- Reviewed 发布操作台已具备确认/撤回/复审 API，但当前前端仍主要展示候选与状态，后续可扩展为独立治理页面。
- APScheduler 长期调度入口已具备 plan、run-once、daemon、scheduler-health、health alerts、recovery runbook 和项目内 launchd/systemd 服务配置生成；当前尚未自动安装到本机系统服务位置，避免无审计修改用户环境。
- AI 反馈和趋势画像有安全降级路径，失败根因报告可定位 Provider/安全/降级簇；系统不能替代心理治疗、法律建议、危机干预或现实专业支持。
- schema guard 已覆盖缺表、缺列和 JSON 质量；正式 migration runner 已提供计划、dry-run、备份、审计、元数据向量索引重建和导入 issue 状态归一化 active revision，复杂历史改列/删列/大规模数据重算仍需继续逐项实现。
- 资源库已完成精确重复治理，当前 12000 条资源对应 12000 个唯一内容指纹；后续仍需继续做语义级近重复治理和真实网页链接周期审计，不应通过无效资源硬凑数量。
- 当前使命对齐评分为 `avg_mission_alignment=82.9`，仍有 76 条低分历史/测试/旧导入资源保留用于兼容与审计；后续可继续做 reviewed/published 分层隐藏或迁移归档。

## 下一轮进化

1. 继续做非严重功能性修复：页面 NaN/空态、按钮交互、启动提示、移动端细节和核心训练路径体验。
2. 保持 `start.sh`、前端 build、关键 API 和核心页面浏览器检查稳定可用。
3. 等项目功能全面定型后，再集中推进 Provider live probe、生产调度安装、严格安全审计和更深的数据治理。
